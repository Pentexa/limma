name: Backend CI

on:
  push:
    branches: [ "main", "master" ]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-ci.yml'
  pull_request:
    branches: [ "main", "master" ]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-ci.yml'

jobs:
  build_and_test:
    name: Build & Test (Rust)
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./backend

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
          POSTGRES_DB: limma
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: clippy, rustfmt

      - name: Fix LD_LIBRARY_PATH format
        run: echo "LD_LIBRARY_PATH=/home/runner/.rustup/toolchains/stable-x86_64-unknown-linux-gnu/lib" >> "$GITHUB_ENV"

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ./backend

      - name: Check formatting
        run: cargo fmt --check

      - name: Lint with Clippy
        run: cargo clippy -- -D warnings

      - name: Run tests
        run: cargo test
        env:
          DATABASE_URL: postgres://postgres:password@127.0.0.1:5432/limma?sslmode=disable

  security_audit:
    name: Dependency Security Audit
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./backend
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Fix LD_LIBRARY_PATH format
        run: echo "LD_LIBRARY_PATH=/home/runner/.rustup/toolchains/stable-x86_64-unknown-linux-gnu/lib" >> "$GITHUB_ENV"

      - name: Install cargo-binstall
        uses: cargo-bins/cargo-binstall@main

      - name: Install cargo-audit
        run: cargo binstall cargo-audit -y --force

      - name: Run cargo audit
        run: cargo audit
        continue-on-error: true
